const { default: axios } = require('axios');
const { run } = require('./backend/database/database');

function saveToDatabase(params) {
  insertRowToDatabase();
}

function insertRowToDatabase(params) {

}

function getRowsFromDatabase(params) {}

function buildUrlForApi(params) {
    
}